{
  "name": "devops-sample-app",
  "version": "1.0.0",
  "private": true,
  "description": "Sample Express app for DevOps CI/CD training",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "build": "rimraf dist && mkdir -p dist && cp -r src dist && cp package.json dist/",
    "test": "jest --coverage",
    "lint": "eslint ."
  },
  "keywords": [
    "devops",
    "ci",
    "cicd",
    "training"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-plugin-node": "^11.1.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.4",
    "rimraf": "^5.0.5"
  }
}
